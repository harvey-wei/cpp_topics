#ifndef FUNC_H
#define FUNC_H

// default values are specified only once in either the declaration or definition, header files whereas initializer list are written in the definition!!
void fun(int i, int j=5, int k=10);
// As with the default values in Python, the default valaues in CPP should start from the leftmost paramter without skip!
#endif
